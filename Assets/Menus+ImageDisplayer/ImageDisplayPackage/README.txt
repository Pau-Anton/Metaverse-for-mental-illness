Image Displayer Package:

Only Valid to use while combined with Menu Package (DBManager needed).

To implement on a 3D scene, copy "canvas" Game Object and set "render mode" to World Space.
Link the camara used in the scene.